/*
 * Used by both VSCode & Gulp-ESLint (where this config file is imported)
 */
{
    "extends": [
        "react-app"
    ],
    "ignorePatterns": [
        "./src/test-util/*.ts(x)",
        "./schematic/*.ts(x)"
    ]
}